name: Test
on:
  # scheduled submission every Wednesday at 1.30 a.m. 
  # schedule:
  #   - cron: '30 5 * * 3'  

  workflow_dispatch:

  
jobs:
  test_job:
    runs-on: ubuntu-latest
    env:
      run_import: 'go'
    
    steps:
     - name: change the var value
       run: |
         echo "Var value is ${{ test_job.env.run_import }} 
