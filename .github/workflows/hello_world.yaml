# workflow name
name: test workflow

# event type
on:
  push:
    branches: [ main ]
#   pull_request:
#     branches:
#       - main
#   workflow_dispatch:
#

jobs:
  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Access
        run: python .github/scripts/verify_access.py
        shell: sh
        
      - name: Say Goodbye
        run: echo "Goodbye world"
        shell: bash
